<ResourceDictionary 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <ResourceDictionary.MergedDictionaries>
        <ResourceDictionary Source="Colors.xaml" />
    </ResourceDictionary.MergedDictionaries>

    <Style TargetType="ToggleButton">
        <Setter Property="Width" Value="50" />
        <Setter Property="Height" Value="25" />
        <Setter Property="Background" Value="DarkGray" />
        <Setter Property="BorderBrush" Value="Green" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ToggleButton">
                    <Border
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="1"
                        Background="{TemplateBinding Background}"
                        CornerRadius="10.5"
                        IsEnabled="{TemplateBinding IsEnabled}">
                        <Ellipse
                            x:Name="ep"
                            Width="15"
                            Height="15"
                            Margin="3"
                            HorizontalAlignment="Left"
                            Fill="LightGray"
                            Stroke="White"
                            StrokeThickness="2" />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsChecked" Value="true">
                            <Setter TargetName="ep" Property="HorizontalAlignment" Value="Right" />
                            <Setter TargetName="ep" Property="Fill" Value="White" />
                            <Setter Property="Background" Value="{StaticResource InertGas.Accent}" />
                        </Trigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False" />
                                <Condition Property="IsChecked" Value="True" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ep" Property="Fill" Value="Gray" />
                            <Setter TargetName="ep" Property="Stroke" Value="LightGray" />
                            <Setter Property="Background" Value="{StaticResource InertGas.DarkAccent}" />
                            <Setter Property="BorderBrush" Value="{StaticResource InertGas.LightRed}" />
                        </MultiTrigger>
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False" />
                                <Condition Property="IsChecked" Value="False" />
                            </MultiTrigger.Conditions>
                            <Setter TargetName="ep" Property="Fill" Value="Gray" />
                            <Setter TargetName="ep" Property="Stroke" Value="LightGray" />
                            <Setter Property="Background" Value="{StaticResource InertGas.DarkGray}" />
                            <Setter Property="BorderBrush" Value="{StaticResource InertGas.LightRed}" />
                        </MultiTrigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
